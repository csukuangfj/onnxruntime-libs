name: ios-shared

on:
  push:
    branches:
      - test-v1.23.2-2
  workflow_dispatch:
    inputs:
      version:
        description: "Version information (e.g., 1.23.2)"
        required: true

permissions:
  contents: write

env:
  ONNXRUNTIME_VERSION:
    |- # Enter release tag name or version name in workflow_dispatch. Appropriate version if not specified
    ${{ github.event.release.tag_name || github.event.inputs.version || '1.23.2' }}

jobs:
  ios-shared:
    name: ios-shared
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest]

    steps:
      - name: Cache build result for ${{ env.ONNXRUNTIME_VERSION }}
        id: cache-build-result
        uses: actions/cache@v4
        with:
          path: onnxruntime-ios-shared-${{ env.ONNXRUNTIME_VERSION }}
          key: onnxruntime-ios-shared-${{ env.ONNXRUNTIME_VERSION }}-cache-v1

      - name: ccache
        uses: hendrikmuhs/ccache-action@v1.2
        with:
          key: ${{ matrix.os }}-ios-shared

      - name: Checkout v${{ env.ONNXRUNTIME_VERSION }}
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        uses: actions/checkout@v4
        with:
          repository: microsoft/onnxruntime
          submodules: true
          ref: v${{ env.ONNXRUNTIME_VERSION }}

      - name: Install dependencies on macos
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        shell: bash
        run: |
          brew install ninja
          which cmake
          cmake --version
          brew install tree
          tree --version

          brew install jq
          which jq

      - name: Patch 1
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        shell: bash
        run: |
          jq '.build_params.base += ["--cmake_extra_defines=CMAKE_POLICY_VERSION_MINIMUM=3.5"]' \
            tools/ci_build/github/apple/default_full_ios_framework_build_settings.json > output.json

          mv output.json tools/ci_build/github/apple/default_full_ios_framework_build_settings.json

          git diff .

      - name: Patch 2
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        shell: bash
        run: |
          # see https://github.com/microsoft/onnxruntime/pull/26380
          #
          jq '.build_params.base += ["--cmake_extra_defines=FLATBUFFERS_BUILD_FLATC=OFF"]' \
            tools/ci_build/github/apple/default_full_ios_framework_build_settings.json > output.json

          mv output.json tools/ci_build/github/apple/default_full_ios_framework_build_settings.json

          git diff .

      - name: Build onnxruntime ${{ env.ONNXRUNTIME_VERSION }} for macOS ${{ matrix.arch }}
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        shell: bash
        run: |
          export CMAKE_CXX_COMPILER_LAUNCHER=ccache
          export PATH="/usr/lib/ccache:/usr/local/opt/ccache/libexec:$PATH"
          cmake --version

          python3 tools/ci_build/github/apple/build_apple_framework.py \
            --config Release \
            --build_dynamic_framework \
            tools/ci_build/github/apple/default_full_ios_framework_build_settings.json

      - name: Create onnxruntime XCFramework
        shell: bash
        run: |
          set -euo pipefail

          CONFIG=Release
          BUILD_ROOT=build/apple_framework/intermediates
          OUT_DIR=dist
          XCFRAMEWORK_NAME=onnxruntime-ios-shared.xcframework

          rm -rf "$OUT_DIR"
          mkdir -p "$OUT_DIR"

          declare -a FRAMEWORK_ARGS=()

          add_framework() {
            local platform=$1
            local arch=$2

            local fw="${BUILD_ROOT}/${platform}_${arch}/${CONFIG}/Release-${platform}/onnxruntime.framework"

            if [[ -d "$fw" ]]; then
              echo "✔ Adding $platform $arch"
              FRAMEWORK_ARGS+=(-framework "$fw")
            else
              echo "⚠ Skipping missing $platform $arch"
            fi
          }

          # iOS device
          add_framework iphoneos arm64

          # iOS simulator
          add_framework iphonesimulator arm64
          add_framework iphonesimulator x86_64

          # Mac Catalyst
          add_framework macabi arm64
          add_framework macabi x86_64

          xcodebuild -create-xcframework \
            "${FRAMEWORK_ARGS[@]}" \
            -output "$OUT_DIR/$XCFRAMEWORK_NAME"

          echo "XCFramework created at $OUT_DIR/$XCFRAMEWORK_NAME"

          tree $OUT_DIR/$XCFRAMEWORK_NAME

      - name: Zip XCFramework
        run: |
          version=${{ env.ONNXRUNTIME_VERSION }}
          cd dist
          zip -r onnxruntime-ios-shared.xcframework-$version.zip onnxruntime-ios-shared.xcframework
          mv *.zip ..

      - name: Upload artifact xcframework
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        uses: actions/upload-artifact@v4
        with:
          name: onnxruntime-ios-shared.xcframework-${{ env.ONNXRUNTIME_VERSION }}
          path: onnxruntime-ios-shared.xcframework-${{ env.ONNXRUNTIME_VERSION }}.zip

      - name: Upload v${{ env.ONNXRUNTIME_VERSION }}
        if: steps.cache-build-result.outputs.cache-hit != 'true'
        uses: svenstaro/upload-release-action@v2
        with:
          file_glob: true
          file: "./*.zip"
          overwrite: true
          tag: v${{ env.ONNXRUNTIME_VERSION }}

      # https://huggingface.co/docs/hub/spaces-github-actions
      - name: Publish to huggingface
        if: github.repository_owner == 'csukuangfj' && steps.cache-build-result.outputs.cache-hit != 'true'
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        uses: nick-fields/retry@v3
        with:
          max_attempts: 20
          timeout_seconds: 200
          shell: bash
          command: |
            rm -rf huggingface

            ls -lh
            version=${{ env.ONNXRUNTIME_VERSION }}

            git config --global user.email "csukuangfj@gmail.com"
            git config --global user.name "Fangjun Kuang"

            export GIT_CLONE_PROTECTION_ACTIVE=false
            rm -rf huggingface
            GIT_LFS_SKIP_SMUDGE=1 git clone https://csukuangfj:$HF_TOKEN@huggingface.co/csukuangfj/onnxruntime-libs huggingface

            cd huggingface
            mkdir -p $version
            cp -v ../onnxruntime-ios-shared.xcframework*.zip ./$version
            git status
            git lfs track "*.zip"

            git add .

            git commit -m "upload onnxruntime-ios-shared.xcframework $version"
            git push https://csukuangfj:$HF_TOKEN@huggingface.co/csukuangfj/onnxruntime-libs main
